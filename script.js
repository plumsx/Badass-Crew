window.addEventListener('load',()=>setTimeout(()=>document.getElementById('loader').classList.add('hide'),1200));
const menu=document.getElementById('menu'),nav=document.getElementById('nav');menu.addEventListener('click',()=>nav.classList.toggle('open'));document.querySelectorAll('nav a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));
const obs=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('show')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>obs.observe(el));
const topBtn=document.getElementById('top');addEventListener('scroll',()=>topBtn.classList.toggle('show',scrollY>500));topBtn.addEventListener('click',()=>scrollTo({top:0,behavior:'smooth'}));
document.getElementById('form').addEventListener('submit',async e=>{e.preventDefault();const d=new FormData(e.target);const msg=`Neue BDss Bewerbung\nSpielername: ${d.get('name')}\nPower: ${d.get('power')}\nDiscord: ${d.get('discord')}\nNachricht: ${d.get('message')}`;try{await navigator.clipboard.writeText(msg);document.getElementById('note').textContent='Bewerbung kopiert. Jetzt im Discord einfügen.'}catch{document.getElementById('note').textContent=msg}});
const c=document.getElementById('particles'),x=c.getContext('2d');let p=[];function resize(){c.width=innerWidth;c.height=innerHeight;p=Array.from({length:Math.min(120,Math.floor(innerWidth/10))},()=>({x:Math.random()*c.width,y:Math.random()*c.height,r:Math.random()*2+0.5,s:Math.random()*0.9+0.2,a:Math.random()*0.7+0.2}))}function draw(){x.clearRect(0,0,c.width,c.height);for(const q of p){q.y-=q.s;if(q.y<0){q.y=c.height;q.x=Math.random()*c.width}x.beginPath();x.fillStyle=`rgba(140,255,42,${q.a})`;x.shadowColor='#8cff2a';x.shadowBlur=16;x.arc(q.x,q.y,q.r,0,Math.PI*2);x.fill()}requestAnimationFrame(draw)}resize();draw();addEventListener('resize',resize);